[project]
name = "calc-cli"
version = "0.1.0"
description = "CLI calculator scaffold"
authors = [
    { name = "TODO" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
